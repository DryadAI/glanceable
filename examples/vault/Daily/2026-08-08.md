---
title: 2026-08-08
tags: [daily, halo]
mood: focused
---

# Thursday

Chord solver work. Read [[Chord Solver]] before touching `typography.py` again —
the **midpoint** trap is easy to reintroduce and it only shows up near the poles.

> [!WARNING] Do not use the midpoint chord
> A line box is a rectangle, so it is constrained by whichever of its two
> horizontal edges sits farther from the equator.

## Today

- [x] Read `brilliant-msg` 7.0.0 properly instead of from memory
- [x] Pin the narrow-edge claim with a test
- [ ] Ask [[Halo SDK|the SDK notes]] whether coords are 1-based
- [ ] Chase down the ==blank screen== report
	- reproduced at 40px with `max_lines=6`
	- clamped by `max_feasible_lines()` now

```dataview
TASK FROM "Daily"
WHERE !completed
SORT file.name DESC
```

## Measured

| size | usable lines | equator | outermost |
|---|---|---|---|
| 11px | 14 | 239px | 30px |
| 13px | 11 | 239px | 65px |
| 16px | 10 | 238px | 68px |

The 24px floor in `max_feasible_lines` is far too generous — 30px is about four
characters. See [[Chord Solver#Line budget]].

%%don't publish the measurement table until it's re-run on hardware%%

![[chord-diagram.png]]

Footnote check[^m].

[^m]: Measured with DejaVu Sans, `safe_inset=8`, on the sample string.
